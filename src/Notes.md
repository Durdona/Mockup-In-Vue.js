<!-- 1. Used CSS Mask on a background image in order make fonts to look like they are behind the mountains  -->

<!-- 2. Used props to pass nav link info from parent to child -->

<!-- 3. Used Vue Router for nav links  -->

<!-- 4. Used  "vue-awesome-swiper": "^4.1.1",  for carousel  but "swiper": "^6.0.2" has a bug didn't work pagination do I had to to downgrade to version "swiper": "^5.4.5" in order pagination to work-->

<!-- 5. I have created a reusable `Menu` component and used it twice inside the component. When need to make changes I had to wrap reusable component in another class and adding more specificity to my CSS was able to target a specific element  -->